# Question 4 — `terraform.tfvars`

[Retour au sommaire](README.md)

```hcl
# terraform.tfvars
environment = "dev"
```

`var.group_name` reçoit la valeur `"photographers"`, car aucune autre valeur ne remplace sa valeur par défaut.
